[CONNECTION]
HOST=127.0.0.1
DATABASE=whatabook  
RAISE_ON_WARNINGS=true

[QUERIES]
SHOW_BOOKS=SELECT book_id, book_name, author, details from book
SHOW_LOCATIONS=SELECT store_id, locale from store
SHOW_USER_IDS=SELECT user_id from user
SHOW_WISHLIST_BOOK=SELECT user.user_id, user.first_name, user.last_name, book.book_id, book.book_name, book.author FROM wishlist INNER JOIN user ON wishlist.user_id = user.user_id INNER JOIN book ON wishlist.book_id = book.book_id WHERE user.user_id = {}
